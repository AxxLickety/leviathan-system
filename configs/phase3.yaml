data:
  input_path: "data/processed/phase3_panel_wret.csv"
  date_col: "date"
  ret_col: "ret_1q_fwd"          # 注意：你现在 panel 里还没有 forward return，需要下一步生成或 merge
features:
  affordability_col: "dti"
  regime_col: "regime"
  supply_cols:
    - "months_supply"
    - "permits"
    - "starts"
phase3:
  lags: [1, 2]
  exposure:
    linear_k: 0.5
    clip_min: 0.0
    clip_max: 1.0
  oos:
    first_test: "2011-03-31"
    step_quarters: 1
